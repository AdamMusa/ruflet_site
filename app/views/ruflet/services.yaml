# Protected device access required by Showcase.
services:
  - camera:
      description: Allows Showcase to capture camera images and scan QR codes.
  - location:
      description: Allows Showcase to read the device location.
